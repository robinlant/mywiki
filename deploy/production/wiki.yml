apiVersion: apps/v1
kind: Deployment
metadata:
  name: wiki
  namespace: maksym
  labels:
    app: wiki
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wiki
  template:
    metadata:
      labels:
        app: wiki
    spec:
      containers:
      - name: mywiki-wiki
        image: robinlant/mywiki-wiki:main.4fcb6
        env:
        - name: ADDR
          value: ":8000"
        - name: MONGO_CON
          value: "mongodb://mongo:27017"
        - name: MONGO_DB
          value: "wiki"
        - name: QUOTES_URL
          value: "http://quotes:8000"
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: "50m"
            memory: "100Mi"
          limits:
            cpu: "250m"
            memory: "500Mi"

---
apiVersion: v1
kind: Service
metadata:
  namespace: maksym
  name: wiki
spec:
  type: ClusterIP
  selector:
    app: wiki
  ports:
  - port: 8000
    targetPort: 8000
